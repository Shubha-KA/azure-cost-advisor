apiVersion: apps/v1
kind: Deployment
metadata:
  name: collection-service
  namespace: {{ .Values.environment.namespace | quote }}
  labels:
    app.kubernetes.io/name: finopsiq
    app.kubernetes.io/component: collection-service
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: finopsiq
      app.kubernetes.io/component: collection-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: finopsiq
        app.kubernetes.io/component: collection-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: collection-service
      containers:
        - name: collection-service
          image: "{{ required "services.collection.image.repository is required" .Values.services.collection.image.repository }}:{{ .Values.services.collection.image.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.services.collection.port }}
          envFrom:
            - secretRef:
                name: {{ .Values.config.secretName | quote }}
          env:
            - name: SERVICE_MODULE
              value: {{ .Values.services.collection.serviceModule | quote }}
            - name: USE_MANAGED_IDENTITY
              value: "true"
            - name: PROCESSING_SERVICE_URL
              value: {{ .Values.config.serviceUrls.processingServiceUrl | quote }}
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu | quote }}
              memory: {{ .Values.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.resources.limits.cpu | quote }}
              memory: {{ .Values.resources.limits.memory | quote }}
